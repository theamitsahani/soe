rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Deny-all: photo storage uses server-signed Cloudinary uploads via Cloud Functions
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}

